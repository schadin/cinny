name: Deploy to GitHub Pages

on:
  release:
    types: [published]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Setup node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version-file: '.node-version'
          package-manager-cache: false
      - name: Install dependencies
        run: npm ci
      - name: Checkout rebrand assets
        run: |
          git clone --depth 1 --branch harrier https://github.com/schadin/harrier-desktop.git /tmp/rebrand
      - name: Apply rebrand
        env:
          REBRAND_CINNY_DIR: ${{ github.workspace }}
        run: node /tmp/rebrand/scripts/rebrand.mjs apply
      - name: Get version
        id: vars
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
      - name: Build app
        env:
          VITE_BASE: /harrier/
          VITE_VERSION: ${{ steps.vars.outputs.version }}
          NODE_OPTIONS: '--max_old_space_size=4096'
        run: npm run build
      - name: Revert rebrand
        if: always()
        env:
          REBRAND_CINNY_DIR: ${{ github.workspace }}
        run: node /tmp/rebrand/scripts/rebrand.mjs revert
      - name: Add .nojekyll
        run: touch dist/.nojekyll
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
